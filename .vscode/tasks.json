{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "SFDX-Save",
            "type": "shell",
            // "command": "clear && prettier --write ${file} && date && sfdx force:source:deploy -p ${file} && date",
            // "command": "prettier --write ${file} && date && sfdx force:source:deploy -p ${file} && date",
            // "command": "date && sfdx force:source:deploy -p ${file} && date",
            "command": "date && sfdx force:source:deploy -p ${file} && date",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}